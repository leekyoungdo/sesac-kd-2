<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <form action="/post" method="post">
        이름<input type="text" name="name" >

        <fieldset>
            <legend>성별</legend>
            <input type="radio" name="gender" value="남자" id="men"><label for="men">남자</label>
            <input type="radio" name="gender" value="여자" id="women"><label for="women">여자</label>
        </fieldset>

        <fieldset>
            <legend>생년월일</legend>

            <select name="year" id="year">
                <% for(var i= 1980;i <=2023;i++) {%>
                <option value="<%= i %>"><%= i %></option>
                    <% } %>
            </select> 년

            <select name="month" id="month">
                <% for(var i= 1; i<=12;i++) {%>
                <option value="<%= i %>"><%= i %></option>
                <% } %>
            </select> 월

            <select name="date" id="date">
                <% for(var i= 1;i <=31;i++) {%>
                <option value="<%= i %>"><%= i %></option>
                    <% } %>
            </select> 일
        </fieldset>
        
        
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="hobby" value="여행" >여행
            <input type="checkbox" name="hobby" value="패션">패션
            <input type="checkbox" name="hobby" value="음식">음식
        </fieldset>
        
        <fieldset>
            <legend>좋아하는것</legend>
            좋아하는 색상 :<input type="color" name="color">
            좋아하는 숫자 :<input type="number" name="number">
        </fieldset>

        <button type="submit">회원가입</button>

    </form>
<hr />


<h1>회원가입 Validation 적용</h1>
<form action="/post" method="post">
    아이디 :<input type="text" name ="id" required pattern="^([a-z0-9]){5,12}$">
    비밀번호 :<input type="password" name = "pw" required pattern="^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{5,10}$">
    <!-- <div id= "error-pw"></div> -->
    이름 :<input type="text" name="name" required>
    전화번호 :<input type="text" name="phoneNumver" oninput="phoneAutoHyphen(this)"
     maxlength="13"
    placeholder="휴대 전화번호"required>


    <br />
    <button type="submit">제출</button>
</form>

<script>
    const phoneAutoHyphen = (target) => {
    target.value = target.value
    .replace(/[^0-9]/g, '')
    .replace(/^(\d{0,3})(\d{0,4})(\d{0,4})$/g, "$1-$2-$3").replace(/(\-{1,2})$/g, "");
}
</script>
<!-- <script>
    function vaildCheckPw(el){
        const regexPw = /^([a-z0-9가*힣]){8,12}$/
        const value = el.value
        const error = document.getElementById("error-pw")
        // regexPw.test("검사하고 싶은값") => true or false return
        if(regexPw.test(value)){
            console.log("정규식 일치")
        }else {
            error.innerHTML = "소문자,숫자로 조합된 8~12자리로 입력"
            console.log("정규식 불일치")
        } 
    }
</script> -->
</body>
</html>

